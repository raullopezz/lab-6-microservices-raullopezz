server:
  port: 8762

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/UNIZAR-30246-WebEngineering/lab6-microservices-config-repo

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/